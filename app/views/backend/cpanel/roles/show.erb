<div class="actionsBar right no-print">
  <%= link_to(image_tag("icons/arrow_left.png"),backend_cpanel_roles_url,:class => "button",:title => "Regresar") %>
  |
  <%= link_to(image_tag("icons/pencil.png"),edit_backend_cpanel_role_url(@role),:class => "button",:title => "Editar") %>
</div>
<hr/>
<div>
  <% semantic_form_remote_for @role, :update => "#{controller.controller_name}_container",:url => "",:html => {:class => "form_generate"} do |f| %>
     <%= render :partial => "form",:locals => {:f => f,:edit => false} %>
  <% end %>
</div>

<script type="text/javascript">
        $(document).ready(function(){

		  $(".security_role input,.security_role textarea").attr("disabled","disabled")

	});

</script>